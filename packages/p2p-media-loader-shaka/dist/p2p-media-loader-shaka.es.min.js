import { Core as e, Core as t, CoreRequestError as n, debug as r } from "p2p-media-loader-core";
//#region src/stream-properties.ts
var i = [
	"mp4a",
	"ac-3",
	"ec-3",
	"ec+3",
	"opus",
	"vorb",
	"flac"
];
function a(e, t) {
	let n = e.bandwidth === 0, r = t.codecs ? t.codecs.split(",").map((e) => e.trim().toLowerCase()).filter((e) => !i.some((t) => e.startsWith(t))).join(",") : void 0, { frameRate: a, hdr: o } = t;
	return {
		bitrate: e.bandwidth,
		codecs: n ? void 0 : r,
		width: n ? void 0 : t.width,
		height: n ? void 0 : t.height,
		frameRate: n ? void 0 : a,
		videoRange: n ? void 0 : o
	};
}
function o(e, t, n) {
	let r = t.label ?? t.originalId ?? void 0;
	return {
		bitrate: n ? e.bandwidth : 0,
		codecs: n ? void 0 : t.codecs,
		language: n ? void 0 : t.language,
		channels: n ? void 0 : t.channelsCount,
		name: n ? void 0 : r
	};
}
//#endregion
//#region src/manifest-parser-decorator.ts
var s = class {
	shaka;
	originalManifestParser;
	debug = r("p2pml-shaka:manifest-parser");
	isHls;
	segmentManager;
	player;
	constructor(e, t) {
		this.shaka = e, this.originalManifestParser = t, this.isHls = this.originalManifestParser instanceof e.hls.HlsParser;
	}
	configure(e) {
		return this.originalManifestParser.configure(e);
	}
	banLocation(e) {
		return this.originalManifestParser.banLocation(e);
	}
	onInitialVariantChosen(e) {
		return this.originalManifestParser.onInitialVariantChosen(e);
	}
	setP2PMediaLoaderData(e) {
		e && (this.segmentManager = e.segmentManager, this.player = e.player, e.streamInfo.protocol = this.isHls ? "hls" : "dash");
	}
	async start(e, t) {
		let { p2pml: n } = t.networkingEngine;
		this.setP2PMediaLoaderData(n);
		let r = await this.originalManifestParser.start(e, t);
		return n ? (this.isHls && this.hookHlsStreamMediaSequenceTimeMaps(r.variants), this.processStreams(r.variants), r) : r;
	}
	stop() {
		return this.originalManifestParser.stop();
	}
	update() {
		return this.originalManifestParser.update();
	}
	setMediaElement(e) {
		return this.originalManifestParser.setMediaElement(e);
	}
	onExpirationUpdated(e, t) {
		return this.originalManifestParser.onExpirationUpdated(e, t);
	}
	processStreams(e) {
		let { segmentManager: t } = this;
		if (!t) return;
		let n = /* @__PURE__ */ new Set(), r = (e, r, i) => {
			try {
				this.hookSegmentIndex(e), t.setStream(e, r, i);
			} catch (t) {
				this.debug(`failed to register stream ${e.id}:`, t);
			}
			return n.add(e.id), !0;
		};
		for (let t of e) {
			let { video: e, audio: i } = t;
			if (e && !n.has(e.id) && r(e, "main", a(t, e)), i && !n.has(i.id)) {
				let n = !e;
				r(i, n ? "main" : "secondary", o(t, i, n));
			}
		}
	}
	hookSegmentIndex(e) {
		let { segmentManager: t } = this;
		if (!t) return;
		let n = (n, r = !1) => {
			let i = null, a, o, s = n.get, c = (l) => {
				let d = s.call(n, l);
				if (d === i || !this.player?.isLive() && e.isSegmentIndexAlreadyRead) return d;
				i = d, n.get = s;
				try {
					let r = u(n, this.shaka);
					if (!r) throw Error("Segment references not found");
					let i = r[0], s = r[r.length - 1];
					if (i === a && s === o) return d;
					a = i, o = s, t.updateStreamSegments(e, r), e.isSegmentIndexAlreadyRead = !0, this.debug(`Stream ${e.id} is updated`);
				} catch {} finally {
					(!e.isSegmentIndexAlreadyRead || this.player?.isLive() || !r) && (n.get = c);
				}
				return d;
			};
			n.get = c;
		};
		if (e.segmentIndex) {
			n(e.segmentIndex);
			return;
		}
		let r = e.createSegmentIndex;
		e.createSegmentIndex = async () => {
			let t = await r.call(e);
			return e.segmentIndex && n(e.segmentIndex, !0), t;
		};
	}
	hookHlsStreamMediaSequenceTimeMaps(e) {
		let t = d(this.originalManifestParser), n, r, i = [
			"video",
			"audio",
			"text",
			"image"
		];
		for (let e of t) i.every((t) => e.has(t)) && (n = e.get("video"), r = e.get("audio"));
		if (n && r) {
			for (let t of e) {
				let { video: e, audio: i } = t;
				e && (e.mediaSequenceTimeMap = n), i && (i.mediaSequenceTimeMap = r);
			}
			return;
		}
		let a = t.find((e) => {
			let t = e.values().next().value;
			return typeof t == "object" && t?.streams?.createSegmentIndex;
		});
		if (!a) return;
		let o = [...a.values()];
		for (let e of o) {
			if (e?.stream?.mediaSequenceTimeMap) continue;
			let t = d(e).find((e) => {
				let [t, n] = e.entries().next().value ?? [];
				return typeof t == "number" && typeof n == "number";
			});
			t && (e.stream.mediaSequenceTimeMap = t);
		}
	}
}, c = class extends s {
	constructor(e) {
		super(e, new e.hls.HlsParser());
	}
}, l = class extends s {
	constructor(e) {
		super(e, new e.dash.DashParser());
	}
};
function u(e, t) {
	for (let n in e) if (Array.isArray(e[n]) && e[n].length > 0 && e[n][0] instanceof t.media.SegmentReference) return e[n];
	else if (typeof e[n] == "object") {
		let r = u(e[n], t);
		if (r) return r;
	}
	return null;
}
function d(e) {
	return Object.keys(e).map((t) => e[t]).filter((e) => e instanceof Map);
}
//#endregion
//#region src/stream-utils.ts
function f({ segmentReference: e, externalId: t, runtimeId: n }) {
	let { byteRange: r, url: i, startTime: a, endTime: o } = g(e);
	return {
		runtimeId: n ?? m(i, r),
		externalId: t,
		byteRange: r,
		url: i,
		startTime: a,
		endTime: o
	};
}
function p(e) {
	let { url: t, byteRange: n } = g(e);
	return m(t, n);
}
function m(e, t) {
	if (!t) return e;
	let n = typeof t == "string" ? h(t) : t;
	return n ? `${e}|${n.start}-${n.end}` : e;
}
function h(e) {
	if (!e?.includes("bytes=")) return;
	let t = e.split("=")[1].split("-"), n = parseInt(t[0]), r = parseInt(t[1]);
	if (!(isNaN(n) || isNaN(r))) return {
		start: n,
		end: r
	};
}
function g(e) {
	let t = e.getUris(), n = t[1] ?? t[0], r = e.getStartByte(), i = e.getEndByte() ?? void 0, a = e.getStartTime(), o = e.getEndTime();
	return {
		byteRange: i === void 0 ? void 0 : {
			start: r,
			end: i
		},
		url: n,
		startTime: a,
		endTime: o
	};
}
function _(e) {
	let { shakaStream: t } = e, n = t.mediaSequenceTimeMap;
	if (!n) return;
	let r = n.keys().next().value;
	if (r !== void 0) return r + n.size - 1;
}
//#endregion
//#region src/segment-manager.ts
var v = .5, y = class {
	core;
	streamInfo;
	constructor(e, t) {
		this.core = t, this.streamInfo = e;
	}
	setStream(e, t, n) {
		this.core.addStreamIfNoneExists({
			runtimeId: e.id.toString(),
			type: t,
			properties: n,
			shakaStream: e
		}), e.segmentIndex && this.updateStreamSegments(e);
	}
	updateStreamSegments(e, t) {
		let n = this.core.getStream(e.id.toString());
		if (!n) return;
		let r = this.core.getStreamSegmentRuntimeIds(n.runtimeId);
		if (!r) return;
		let { segmentIndex: i } = n.shakaStream;
		if (!t && i) try {
			t = [...i].filter((e) => !!e);
		} catch {
			return;
		}
		t && (this.streamInfo.protocol === "hls" ? this.processHlsSegmentReferences(n, r, t) : this.processDashSegmentReferences(n, r, t));
	}
	processDashSegmentReferences(e, t, n) {
		let r = new Set(t), i = [];
		for (let e of n) {
			let n = Math.trunc(e.getStartTime() / v), a = p(e);
			if (!t.has(a)) {
				let t = f({
					segmentReference: e,
					externalId: n,
					runtimeId: a
				});
				i.push(t);
			}
			r.delete(a);
		}
		!i.length && !r.size || this.core.updateStream(e.runtimeId, i, r.values());
	}
	processHlsSegmentReferences(e, t, n) {
		let r = _(e), i = [];
		if (t.size === 0) {
			let t = r === void 0 ? 0 : r - n.length + 1;
			for (let [e, r] of n.entries()) {
				let n = f({
					segmentReference: r,
					externalId: t + e
				});
				i.push(n);
			}
			this.core.updateStream(e.runtimeId, i);
			return;
		}
		if (r === void 0) return;
		let a = r;
		for (let e of b(n)) {
			let n = p(e);
			if (t.has(n)) break;
			let r = f({
				runtimeId: n,
				segmentReference: e,
				externalId: a
			});
			i.push(r), a--;
		}
		i.reverse();
		let o = [], s = i.length;
		for (let e of t) {
			if (o.length >= s) break;
			o.push(e);
		}
		!i.length && !o.length || this.core.updateStream(e.runtimeId, i, o);
	}
};
function* b(e) {
	for (let t = e.length - 1; t >= 0; t--) yield e[t];
}
//#endregion
//#region src/loading-handler.ts
var x = class {
	shaka;
	core;
	streamInfo;
	loadArgs;
	constructor(e, t, n) {
		this.shaka = e, this.core = t, this.streamInfo = n;
	}
	defaultLoad() {
		return this.shaka.net.HttpFetchPlugin.parse(...this.loadArgs);
	}
	load(...e) {
		this.loadArgs = e;
		let { RequestType: t } = this.shaka.net.NetworkingEngine, [n, r, i] = e;
		if (i === t.SEGMENT) return this.loadSegment(n, r);
		let a = this.defaultLoad();
		return i === t.MANIFEST && this.handleManifestLoading(a.promise).catch(() => void 0), a;
	}
	async handleManifestLoading(e) {
		if (!this.streamInfo.manifestResponseUrl) {
			let t = await e;
			this.setManifestResponseUrl(t.uri);
		}
	}
	loadSegment(e, t) {
		let r = t.headers.Range, i = m(e, r), a = this.core.isSegmentLoadable(i);
		return !this.core.hasSegment(i) || !a ? this.defaultLoad() : new this.shaka.util.AbortableOperation((async () => {
			let { request: r, callbacks: a } = C();
			this.core.loadSegment(i, a);
			try {
				let { data: n, bandwidth: i } = await r;
				return {
					data: n,
					headers: {},
					originalRequest: t,
					uri: e,
					originalUri: e,
					timeMs: S(i, n.byteLength)
				};
			} catch (e) {
				if (e instanceof n) {
					let { Error: t } = this.shaka.util;
					if (e.type === "aborted") throw new t(t.Severity.RECOVERABLE, t.Category.NETWORK, this.shaka.util.Error.Code.OPERATION_ABORTED);
				}
				throw e;
			}
		})(), () => (this.core.abortSegmentLoading(i), Promise.resolve()));
	}
	setManifestResponseUrl(e) {
		this.streamInfo.manifestResponseUrl = e, this.core.setManifestResponseUrl(e);
	}
};
function S(e, t) {
	let n = t * 8;
	return e > 0 ? Math.round(n / e) * 1e3 : 0;
}
function C() {
	let e, t;
	return {
		request: new Promise((n, r) => {
			e = n, t = r;
		}),
		callbacks: {
			onSuccess: e,
			onError: t
		}
	};
}
//#endregion
//#region src/engine.ts
var w = 25, T = class e {
	player;
	shaka;
	streamInfo = {};
	core;
	segmentManager;
	requestFilter;
	constructor(e, n = window.shaka) {
		E(n), this.shaka = n, this.core = new t(e?.core), this.segmentManager = new y(this.streamInfo, this.core);
	}
	bindShakaPlayer(e) {
		if (this.player === e) return;
		this.player && this.destroy(), this.player = e, this.player.configure("manifest.defaultPresentationDelay", w), this.player.configure("manifest.dash.ignoreSuggestedPresentationDelay", !0);
		let t = /\d+/.exec(this.shaka.Player.version);
		parseInt(t ? t[0] : "0", 10) >= 5 ? this.player.configure("streaming.preferNativeHls", !1) : this.player.configure("streaming.useNativeHlsOnSafari", !1), this.updatePlayerEventHandlers("register");
	}
	applyDynamicConfig(e) {
		e.core && this.core.applyDynamicConfig(e.core);
	}
	getConfig() {
		return { core: this.core.getConfig() };
	}
	addEventListener(e, t) {
		this.core.addEventListener(e, t);
	}
	removeEventListener(e, t) {
		this.core.removeEventListener(e, t);
	}
	updatePlayerEventHandlers = (e) => {
		let { player: t } = this;
		if (!t) return;
		let n = t.getNetworkingEngine();
		if (n) if (e === "register") {
			let e = {
				player: t,
				shaka: this.shaka,
				core: this.core,
				streamInfo: this.streamInfo,
				segmentManager: this.segmentManager
			};
			this.requestFilter = (t, n) => {
				n.p2pml = e;
			}, n.p2pml = e, n.registerRequestFilter(this.requestFilter);
		} else n.p2pml = void 0, this.requestFilter && n.unregisterRequestFilter(this.requestFilter);
		let r = e === "register" ? "addEventListener" : "removeEventListener";
		t[r]("loaded", this.handlePlayerLoaded), t[r]("loading", this.destroyCurrentStreamContext), t[r]("unloading", this.handlePlayerUnloading), t[r]("adaptation", this.onVariantChanged), t[r]("variantchanged", this.onVariantChanged);
	};
	onVariantChanged = () => {
		if (!this.player) return;
		let e = this.player.getVariantTracks().find((e) => e.active);
		e && this.core.setActiveLevelBitrate(e.bandwidth);
	};
	handlePlayerLoaded = () => {
		this.player && (this.core.setIsLive(this.player.isLive()), this.updateMediaElementEventHandlers("register"));
	};
	handlePlayerUnloading = () => {
		this.destroyCurrentStreamContext(), this.updateMediaElementEventHandlers("unregister");
	};
	destroyCurrentStreamContext = () => {
		this.streamInfo.protocol = void 0, this.streamInfo.manifestResponseUrl = void 0, this.core.destroy();
	};
	updateMediaElementEventHandlers = (e) => {
		let t = this.player?.getMediaElement();
		if (!t) return;
		let n = e === "register" ? "addEventListener" : "removeEventListener";
		t[n]("timeupdate", this.handlePlaybackUpdate), t[n]("ratechange", this.handlePlaybackUpdate), t[n]("seeking", this.handlePlaybackUpdate);
	};
	handlePlaybackUpdate = (e) => {
		let t = e.target;
		this.core.updatePlayback(t.currentTime, t.playbackRate);
	};
	destroy() {
		this.destroyCurrentStreamContext(), this.updatePlayerEventHandlers("unregister"), this.updateMediaElementEventHandlers("unregister"), this.player = void 0;
	}
	static registerManifestParsers(e) {
		let t = () => new c(e), n = () => new l(e), r = e.media.ManifestParser;
		r.registerParserByMime("application/dash+xml", n), r.registerParserByMime("application/x-mpegurl", t), r.registerParserByMime("application/vnd.apple.mpegurl", t);
	}
	static unregisterManifestParsers(e) {
		let t = e.media.ManifestParser;
		t.unregisterParserByMime("mpd"), t.unregisterParserByMime("application/dash+xml"), t.unregisterParserByMime("m3u8"), t.unregisterParserByMime("application/x-mpegurl"), t.unregisterParserByMime("application/vnd.apple.mpegurl");
	}
	static registerNetworkingEngineSchemes(e) {
		let { NetworkingEngine: t } = e.net, n = (...t) => {
			let { p2pml: n } = t[1];
			return n ? new x(n.shaka, n.core, n.streamInfo).load(...t) : e.net.HttpFetchPlugin.parse(...t);
		};
		t.registerScheme("http", n), t.registerScheme("https", n), t.registerScheme("data", n);
	}
	static unregisterNetworkingEngineSchemes(e) {
		let { NetworkingEngine: t } = e.net;
		t.unregisterScheme("http"), t.unregisterScheme("https"), t.unregisterScheme("data");
	}
	static registerPlugins(t = window.shaka) {
		E(t), e.registerManifestParsers(t), e.registerNetworkingEngineSchemes(t);
	}
	static unregisterPlugins(t = window.shaka) {
		E(t), e.unregisterManifestParsers(t), e.unregisterNetworkingEngineSchemes(t);
	}
};
function E(e) {
	if (!e) throw Error("shaka namespace is not defined in global scope and not passed as an argument to Shaka P2P engine constructor");
}
//#endregion
export { e as Core, T as ShakaP2PEngine };

//# sourceMappingURL=p2p-media-loader-shaka.es.min.js.map